# CADENCE — copy to `.env` and fill in. Never commit your real `.env`.
# Every value below is a placeholder; nothing here ties to any account.

CADENCE_DIR=   # project directory to scan; blank = current dir (or pass --dir=)
CADENCE_DATA_DIR=   # optional override for the ./data directory; blank = <repo>/data
